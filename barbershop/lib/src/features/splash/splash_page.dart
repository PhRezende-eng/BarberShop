import 'package:flutter/material.dart';

class BSSplashPage extends StatelessWidget {
  const BSSplashPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
